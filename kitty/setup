#!/usr/bin/env bash

set -e

KITTY="$HOME/.config/kitty"

mkdir -p "$KITTY"

# create symlinks for the config & theme files
ln -sf "$HOME/github.com/odas0r/dot/kitty/kitty.conf" "$KITTY/kitty.conf"
ln -sf "$HOME/github.com/odas0r/dot/kitty/themes" "$KITTY/themes"

echo "Kitty setup done! ⭐"
