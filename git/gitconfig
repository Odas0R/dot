[user]
  name = odas0R
  email = g.rosado@campus.fct.unl.pt
[push]
  followTags = true
	default = current
[core]
  editor = nvim
	autocrlf = false
[init]
	defaultBranch = main
[diff]
    tool = nvr
[difftool "nvr"]
    cmd = nvr -s -d $LOCAL $REMOTE
[merge]
    tool = nvr
[mergetool "nvr"]
    cmd = nvr -s -d $LOCAL $BASE $REMOTE $MERGED -c 'wincmd J | wincmd ='
    prompt = false
    keepBackup = false

; [merge]
; 	tool = "nvim -d"
; 	conflictstyle = diff3
; [mergetool]
;   prompt = false
;   keepBackup = false
