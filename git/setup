#!/usr/bin/env bash

set -euo pipefail

declare DISTRO
declare -r GIT_PATH="$HOME/dot/git"

if [[ "$OSTYPE" == "darwin"* ]]; then
  # macOS OSX
  ln -sf "$GIT_PATH/gitconfig_darwin" "$HOME/.gitconfig"
else
  ln -sf "$GIT_PATH/gitconfig" "$HOME/.gitconfig"
fi

printf "Git setup done! 🌟\n"
