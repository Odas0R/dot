#!/usr/bin/env bash

set -euo pipefail

chars=${1:-64}

[[ "$chars" =~ ^[0-9]+$ ]] || {
  echo "usage: secret [hex_chars]" >&2
  exit 2
}

bytes=$(((chars + 1) / 2))

openssl rand -hex "$bytes" | cut -c "1-$chars"
